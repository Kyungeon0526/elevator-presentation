# 🎯 ESP32-S3OV3660 카메라 설정 가이드

## 🚨 문제 원인

**"Detected camera not supported" 에러의 원인:**
- 기존 코드는 AI Thinker ESP32-CAM 모델 핀 설정
- 사용하시는 모델: ESP32-S3OV3660
- 서로 다른 핀 맵 사용 → 카메라 초기화 실패

---

## ✅ 해결된 핀 설정

### ESP32-S3OV3660 올바른 핀 맵

```cpp
// 카메라 핀 설정 (ESP32-S3OV3660)
#define PWDN_GPIO_NUM     -1
#define RESET_GPIO_NUM    -1
#define XCLK_GPIO_NUM     40
#define SIOD_GPIO_NUM     18
#define SIOC_GPIO_NUM     17

#define Y9_GPIO_NUM       39
#define Y8_GPIO_NUM       41
#define Y7_GPIO_NUM       42
#define Y6_GPIO_NUM       45
#define Y5_GPIO_NUM       48
#define Y4_GPIO_NUM       35
#define Y3_GPIO_NUM       36
#define Y2_GPIO_NUM       37
#define VSYNC_GPIO_NUM    47
#define HREF_GPIO_NUM     39
#define PCLK_GPIO_NUM     21
```

---

## 🔧 Arduino CLI 설정 명령어

### 1. 보드 설치 확인

```bash
# ESP32 보드 설치 확인
.\arduino-cli.exe core list

# ESP32 보드가 없으면 설치
.\arduino-cli.exe core install esp32:esp32
```

### 2. 올바른 보드 선택 (중요! ⭐)

ESP32-S3OV3660을 위한 올바른 보드 선택:

```bash
# ESP32-S3 보드 모델 확인
.\arduino-cli.exe board listall | findstr "ESP32-S3"

# ESP32-S3OV3660에 맞는 보드 선택 (가장 가까운 모델)
.\arduino-cli.exe compile --fqbn esp32:esp32:esp32s3 elevator_camera.ino

# 또는 AI Thinker ESP32-S3 보드 사용
.\arduino-cli.exe compile --fqbn esp32:esp32:esp32s3cam esp32_vision/elevator_camera.ino
```

### 3. 업로드

```bash
# ESP32-S3OV3660 업로드
.\arduino-cli.exe upload -p COM7 --fqbn esp32:esp32:esp32s3 esp32_vision/elevator_camera.ino
```

---

## 🔄 대체 핀 설정 (위 설정이 작동하지 않을 때)

### ESP32-S3 다른 핀 맵 시도

```cpp
// 대체 핀 설정 1
#define PWDN_GPIO_NUM     -1
#define RESET_GPIO_NUM    -1
#define XCLK_GPIO_NUM     15
#define SIOD_GPIO_NUM     4
#define SIOC_GPIO_NUM     5

#define Y9_GPIO_NUM       16
#define Y8_GPIO_NUM       17
#define Y7_GPIO_NUM       18
#define Y6_GPIO_NUM       12
#define Y5_GPIO_NUM       10
#define Y4_GPIO_NUM       8
#define Y3_GPIO_NUM       9
#define Y2_GPIO_NUM       11
#define VSYNC_GPIO_NUM    6
#define HREF_GPIO_NUM    7
#define PCLK_GPIO_NUM    13
```

---

## 📋 업로드 절차

### 1. 현재 코드 업로드

```bash
# 컴파일
cd C:\Users\LG\Desktop\phsical_AI\esp32_vision
.\..\arduino-cli_1.5.2-rc.1_Windows_64bit\arduino-cli.exe compile --fqbn esp32:esp32:esp32s3 elevator_camera.ino

# 업로드 (GPIO0 연결 필요)
.\..\arduino-cli_1.5.2-rc.1_Windows_64bit\arduino-cli.exe upload -p COM7 --fqbn esp32:esp32:esp32s3 elevator_camera.ino
```

### 2. GPIO0 연결 (업로드 전)

```
1. ESP32의 GPIO0 핀을 GND에 연결
2. EN(Reset) 버튼 클릭
3. 업로드 명령 실행
4. 업로드 완료 후 GPIO0 연결 해제
5. EN 버튼 다시 클릭
```

### 3. 시리얼 모니터로 확인

```bash
# 모니터 시작
.\..\arduino-cli_1.5.2-rc.1_Windows_64bit\arduino-cli.exe monitor -p COM7 -c baudrate=115200
```

---

## 🎯 예상되는 정상 결과

```
ESP32 엘리베이터 숫자 인식 시스템 시작...
UTF-8 인코딩 활성화
ESP32 AP 모드 시작...
AP 모드 시작 성공!
SSID: ESP32-Sense
비밀번호: 12345678
IP 주소: 192.168.4.1

📱 연결 방법:
1. 스마트폰 WiFi 설정 열기
2. 'ESP32-Sense' 와이파이 선택
3. 비밀번호: 12345678 입력
4. 브라우저에서 http://192.168.4.1 접속

카메라 초기화 완료  ✅ (여기가 중요!)
웹서버 시작
서버 주소: http://192.168.4.1
시스템 준비 완료
```

---

## 🛠️ 여전히 에러가 발생하면

### 에러 1: "Detected camera not supported" 계속 발생

```bash
# 1. 다른 보드 모델 시도
.\arduino-cli.exe board listall | findstr "ESP32-S3"

# 2. 가능한 보드 모델들:
# esp32:esp32:esp32s3cam
# esp32:esp32:esp32s3
# esp32:esp32:esp32-c3-devkitm-1

# 3. 각 모델로 시도
.\arduino-cli.exe upload -p COM7 --fqbn esp32:esp32:esp32s3cam elevator_camera.ino
```

### 에러 2: "카메라 초기화 실패: 0x105"

```cpp
// 핀 설정을 대체 핀 맵으로 변경 후 재시도
```

### 에러 3: 컴파일 에러

```bash
# ESP32 코어 업데이트
.\arduino-cli.exe core update esp32:esp32
```

---

## 📸 테스트 단계

### 1단계: 카메라 초기화 확인

```
시리얼 모니터에서 "카메라 초기화 완료" 메시지 확인
```

### 2단계: 웹페이지 접속

```
1. WiFi "ESP32-Sense" 연결
2. http://192.168.4.1 접속
3. 한국어가 정상적으로 표시되는지 확인
```

### 3단계: 카메라 이미지 확인

```
1. http://192.168.4.1/capture 접속
2. 카메라 이미지가 보이는지 확인
3. 이미지 해상도 확인 (160x120)
```

### 4단계: API 테스트

```
1. http://192.168.4.1/api/status 접속
2. JSON 데이터 확인:
   {
     "floor": 1,
     "direction": "stopped",
     "processingTime": 0
   }
```

---

## 🔍 디버깅 명령어

### 사용 가능한 보드 목록 확인

```bash
.\arduino-cli.exe board listall | findstr "ESP32"
```

### 현재 연결된 보드 확인

```bash
.\arduino-cli.exe board list
```

### 코어 버전 확인

```bash
.\arduino-cli.exe core list
```

---

## 💡 팁

1. **GPIO0 연결은 업로드에 필수!**
2. **전력 안정성이 중요** - 5V 어댑터 권장
3. **USB 케이블 확인** - 데이터 전송용 사용
4. **보드 모델 정확히 선택** - ESP32-S3 계열
5. **핀 설정 모델에 맞추기** - ESP32-S3OV3660

---

## 📞 문제 해결 시 필요 정보

이 문제를 해결하기 위해 다음 정보가 필요합니다:

1. **Arduino CLI 버전**: `.\arduino-cli.exe version`
2. **ESP32 코어 버전**: `.\arduino-cli.exe core list`
3. **사용 가능한 보드 목록**: `.\arduino-cli.exe board listall | findstr "ESP32-S3"`
4. **업로드 후 시리얼 모니터 출력 전체**

---

**⏱️ 예상 해결 시간: 5-10분**

**🎯 목표: "카메라 초기화 완료" 메시지 확인**
