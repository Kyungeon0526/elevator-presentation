# 🔍 ESP32 기본 기능 진단 가이드

## 🚨 긴급 진단 체크리스트

### 1단계: 시리얼 모니터 확인 (가장 중요! ⭐)

```
1. Arduino IDE 열기
2. 시리얼 모니터 클릭 (돋보기 아이콘 또는 Ctrl+Shift+M)
3. Baud rate: 115200 선택
4. 아래와 같은 메시지가 보이는지 확인:

✅ 정상적인 경우:
```
ESP32 엘리베이터 숫자 인식 시스템 시작...
UTF-8 인코딩 활성화
ESP32 AP 모드 시작...
AP 모드 시작 성공!
SSID: ESP32-Sense
비밀번호: 12345678
IP 주소: 192.168.4.1

📱 연결 방법:
1. 스마트폰 WiFi 설정 열기
2. 'ESP32-Sense' 와이파이 선택
3. 비밀번호: 12345678 입력
4. 브라우저에서 http://192.168.4.1 접속

카메라 초기화 완료
웹서버 시작
서버 주소: http://192.168.4.1
시스템 준비 완료
```

❌ 문제가 있는 경우:
- 아무것도 안 보임 → ESP32가 시작되지 않음
- "카메라 초기화 실패" → 카메라 연결 문제
- "AP 모드 시작 실패" → WiFi 문제
- IP 주소가 없음 → 네트워크 문제

---

### 2단계: 전력 공급 확인

⚡ **ESP32-CAM은 전력 부족에 매우 민감합니다!**

**문제 증상:**
- 시리얼 모니터에서 아무것도 안 보임
- 부팅되다가 멈춤
- WiFi 연결 실패

**해결 방법:**
```
1. 안정적인 5V 전원 어댑터 사용 (USB 포트 전력 불충분)
2. USB 케이블 교체 (충전 전용 케이블 제외, 데이터 전송용 사용)
3. 전원 LED가 밝게 켜져 있는지 확인
4. 가능하다면 외부 5V 전원 어댑터 사용
```

---

### 3단계: 카메라 연결 확인

**문제 증상:**
- "카메라 초기화 실패: 0x105" 메시지
- 웹페이지에서 이미지가 안 보임

**해결 방법:**
```
1. 카메라 모듈이 ESP32에 제대로 꽂혔는지 확인
2. FPC 케이블 연결 상태 확인 (끊어짐, 느슨함)
3. 카메라 모듈 교체 시도
```

---

### 4단계: WiFi 연결 확인

**AP 모드로 동작하므로 직접 연결:**

```
1. 스마트폰/노트북 WiFi 설정 열기
2. "ESP32-Sense" 네트워크 찾기
3. 비밀번호: 12345678 입력
4. 연결 성공 확인
```

**연결이 안 되는 경우:**
```
1. ESP32 재시작 (리셋 버튼 클릭)
2. 10초 정도 기다린 후 다시 시도
3. 다른 기기로 연결 시도
4. 시리얼 모니터에서 "AP 모드 시작 성공!" 메시지 확인
```

---

### 5단계: 웹서버 접속 테스트

```
1. http://192.168.4.1 접속
2. 아무것도 안 뜸 → 웹서버가 시작되지 않음
3. 연결 거부 → 방화벽 문제 또는 ESP32 재시작 필요
4. 페이지 로드됨 → 기본 기능 정상
```

---

## 🛠️ 문제별 해결 방법

### 문제 1: 시리얼 모니터에 아무것도 안 보임

**가능한 원인:**
1. COM 포트 선택이 틀림
2. ESP32가 부팅되지 않음
3. 전력 부족
4. USB 케이블 문제

**해결 순서:**
```
1. Tools → Port에서 올바른 COM 포트 선택
2. USB 케이블 교체 (데이터 전송용 확인)
3. 안정적인 5V 전원 어댑터 사용
4. EN(Reset) 버튼 여러 번 클릭
5. GPIO0을 GND에 연결한 상태에서 업로드 재시도
```

---

### 문제 2: "카메라 초기화 실패" 메시지

**가능한 원인:**
1. 카메라 모듈 연결 불량
2. 전력 부족
3. 잘못된 핀 설정

**해결 순서:**
```
1. 카메라 모듈 재연결 (FPC 케이블 확인)
2. 전력 어댑터 사용
3. 카메라 모듈 교체
4. 코드의 핀 설정 확인 (AI Thinker 모델인지)
```

---

### 문제 3: WiFi가 안 잡힘

**가능한 원인:**
1. ESP32가 AP 모드로 시작되지 않음
2. 전력 부족
3. WiFi 라이브러리 문제

**해결 순서:**
```
1. 시리얼 모니터에서 "AP 모드 시작 성공!" 확인
2. ESP32 재시작 (리셋 버튼)
3. 다른 기기로 연결 시도
4. WiFi 채널 변경 (코드에서는 기본값 사용)
```

---

### 문제 4: 웹페이지 접속 안 됨

**가능한 원인:**
1. 웹서버가 시작되지 않음
2. 방화벽 문제
3. 브라우저 캐시

**해결 순서:**
```
1. 시리얼 모니터에서 "웹서버 시작" 확인
2. 브라우저 캐시 초기화 (Ctrl+F5)
3. 다른 브라우저 시도
4. 방화벽 일시 해제
5. http://192.168.4.1 (http:// 확인)
```

---

## 🚀 빠른 진단 방법

### 방법 1: 진단 페이지 사용

```
1. elevator-monitor 폴더에서 웹서버 시작:
   python -m http.server 8000

2. 브라우저에서:
   http://localhost:8000/test-connection.html

3. ESP32 URL 입력: http://192.168.4.1
4. "연결 테스트" 버튼 클릭
```

### 방법 2: 브라우저 콘솔에서 진단

```
1. 개발자 도구 열기 (F12)
2. Console 탭 선택
3. diagnostic.js 내용 복사해서 실행
4. 진단 결과 확인
```

---

## 📋 최종 체크리스트

진단 전 필수 확인:
- [ ] 안정적인 5V 전원 공급
- [ ] 데이터 전송용 USB 케이블
- [ ] 올바른 COM 포트 선택
- [ ] Baud rate 115200 설정
- [ ] Arduino IDE 최신 버전

진단 후 확인:
- [ ] 시리얼 모니터에 "시스템 준비 완료" 메시지
- [ ] WiFi "ESP32-Sense" 네트워크 연결
- [ ] http://192.168.4.1 웹페이지 접속
- [ ] 카메라 캡처 작동 (/capture)
- [ ] API 응답 (/api/status)

---

## 🆘 응급 조치

**아무것도 안 되는 경우:**

```
1. 모든 것 분리하고 10초 대기
2. 새로운 USB 케이블 사용
3. 5V 전원 어댑터 사용
4. GPIO0-GND 연결 후 업로드
5. 업로드 완료 후 GPIO0 연결 해제
6. 리셋 버튼 클릭
7. 시리얼 모니터 확인
```

**그래도 안 되면:**
- ESP32 보드 교체
- 카메라 모듈 교체
- Arduino IDE 재설치
- 다른 컴퓨터에서 시도

---

## 📞 지원 요청 시 필요 정보

이 문제 해결을 위해 다음 정보를 제공해 주세요:

1. **시리얼 모니터 출력 전체 복사**
2. **Arduino IDE 버전**
3. **사용 중인 ESP32 보드 모델**
4. **전원 공급 방법** (USB 포트 vs 외부 어댑터)
5. **WiFi 연결 상태**
6. **브라우저에서 발생하는 정확한 에러 메시지**

---

**⏱️ 예상 진단 시간: 10-15분**

**🎯 목표: 시리얼 모니터에서 "시스템 준비 완료" 메시지 확인**
